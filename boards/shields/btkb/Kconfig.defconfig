if SHIELD_BTKB_LEFT

# Name must be less than 16 characters long!
config ZMK_KEYBOARD_NAME
    default "Nickacide kb"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_BTKB_LEFT || SHIELD_BTKB_RIGHT

config ZMK_SPLIT
    default y

endif
